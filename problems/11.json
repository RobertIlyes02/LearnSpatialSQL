{
  "id": 11,
  "title": "Neighbourhood Centroids",
  "diff": "Easy",
  "acc": 74,
  "topic": "Geometry",
  "tags": ["ST_Centroid", "ST_X", "ST_Y"],
  "premium": false,
  "hasExecution": true,
  "description": "<p>A city stores its <strong>neighbourhood boundaries</strong> as polygons. Urban planners need the centre point (centroid) of each neighbourhood to place service facilities.</p><h3>Goal</h3><p>Return <code>name</code>, <code>cx</code> (centroid longitude, rounded to 4 decimal places), and <code>cy</code> (centroid latitude, rounded to 4 decimal places) for every neighbourhood, ordered by <code>name</code>.</p><h3>Example Output</h3><pre>name           cx       cy\n-------------  -------  ------\nEastside       25.0     2.6667\nWestside        5.0     3.3333</pre>",
  "schema": "<h3>Table: neighbourhoods</h3><table class=\"schema-table\"><tr><th>Column</th><th>Type</th><th>Description</th></tr><tr><td>name</td><td>VARCHAR</td><td>Neighbourhood name</td></tr><tr><td>geom</td><td>GEOMETRY</td><td>Boundary polygon</td></tr></table><p style=\"margin-top:0.75rem\"><code>ST_Centroid(geom)</code> returns a POINT geometry at the geometric centre of the input. Use <code>ST_X()</code> and <code>ST_Y()</code> to extract the coordinates.</p>",
  "hints": "<div class=\"hint-block\"><strong>💡 Hint 1</strong> Chain the functions: <code>ST_X(ST_Centroid(geom))</code> gives you the x-coordinate of the centroid.</div><div class=\"hint-block\" style=\"margin-top:0.75rem\"><strong>💡 Hint 2</strong> Wrap with <code>ROUND(..., 4)</code> to match the expected output precision.</div>",
  "starterCode": "LOAD spatial;\n\n-- Return the centroid coordinates of each neighbourhood.\n-- Return: name, cx (rounded 4dp), cy (rounded 4dp) ordered by name\nSELECT\n  name,\n\nFROM neighbourhoods\nORDER BY name;",
  "setup": "CREATE TABLE neighbourhoods AS SELECT * FROM (VALUES\n  ('Westside', ST_GeomFromText('POLYGON((0 0, 10 0, 5 10, 0 0))')),\n  ('Eastside', ST_GeomFromText('POLYGON((20 0, 30 0, 25 8, 20 0))')\n  )\n) t(name, geom);",
  "expected": [
    { "name": "Eastside", "cx": 25.0, "cy": 2.6667 },
    { "name": "Westside", "cx": 5.0,  "cy": 3.3333 }
  ]
}
