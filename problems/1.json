{
  "id": 1,
  "title": "Point in Polygon",
  "diff": "Easy",
  "topic": "Predicates",
  "tags": [
    "ST_Within",
    "Polygon"
  ],
  "premium": false,
  "description": "<p>You have a table of <strong>city parks</strong> (polygons) and a table of <strong>incident reports</strong> (points). Find all incidents that fall <em>inside</em> any park.</p><h3>Goal</h3><p>Return the <code>incident_id</code> and <code>park_name</code> for every incident that lies within a park boundary, ordered by <code>incident_id</code>.</p><h3>Example Output</h3><pre>incident_id  park_name\n-----------  --------------------\n1            Riverside Park\n3            Central Commons\n5            Riverside Park</pre>",
  "schema": "<h3>Tables</h3><table class=\"schema-table\"><tr><th>Column</th><th>Type</th><th>Description</th></tr><tr><td>parks.park_id</td><td>INTEGER</td><td>Primary key</td></tr><tr><td>parks.park_name</td><td>VARCHAR</td><td>Name of park</td></tr><tr><td>parks.geom</td><td>GEOMETRY</td><td>Polygon boundary (EPSG:4326)</td></tr><tr><td>incidents.incident_id</td><td>INTEGER</td><td>Primary key</td></tr><tr><td>incidents.geom</td><td>GEOMETRY</td><td>Point location</td></tr></table><h3>DuckDB Spatial Functions</h3><p>Load the extension first: <code>LOAD spatial;</code></p><p>Use <code>ST_Within(point, polygon)</code> to test containment, or <code>ST_Intersects</code>.</p>",
  "hints": "<div class=\"hint-block\"><strong>💡 Hint 1</strong> Join the two tables with a spatial predicate instead of an equality condition.</div><div class=\"hint-block\" style=\"margin-top:0.75rem\"><strong>💡 Hint 2</strong> <code>ST_Within(a.geom, b.geom)</code> returns TRUE when geometry A is completely inside B.</div><div class=\"hint-block\" style=\"margin-top:0.75rem\"><strong>💡 Hint 3</strong> Don't forget <code>LOAD spatial;</code> at the top of your query.</div>",
  "starterCode": "LOAD spatial;\n\n-- Find all incidents that fall inside a park.\n-- Return: incident_id, park_name ordered by incident_id\nSELECT\n\nFROM\n\nORDER BY incident_id;",
  "setup": "CREATE TABLE parks AS SELECT * FROM (VALUES\n  (1, 'Riverside Park',   ST_GeomFromText('POLYGON((-73.99 40.71, -73.97 40.71, -73.97 40.73, -73.99 40.73, -73.99 40.71))')),\n  (2, 'Central Commons',  ST_GeomFromText('POLYGON((-74.01 40.74, -73.99 40.74, -73.99 40.76, -74.01 40.76, -74.01 40.74))'))\n) t(park_id, park_name, geom);\n\nCREATE TABLE incidents AS SELECT * FROM (VALUES\n  (1, ST_Point(-73.98,  40.72)),\n  (2, ST_Point(-74.05,  40.70)),\n  (3, ST_Point(-74.00,  40.75)),\n  (4, ST_Point(-73.95,  40.68)),\n  (5, ST_Point(-73.975, 40.715))\n) t(incident_id, geom);",
  "expected": [
    {
      "incident_id": 1,
      "park_name": "Riverside Park"
    },
    {
      "incident_id": 3,
      "park_name": "Central Commons"
    },
    {
      "incident_id": 5,
      "park_name": "Riverside Park"
    }
  ]
}
