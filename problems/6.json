{
  "id": 6,
  "title": "Convex Hull of Cluster",
  "diff": "Medium",
  "topic": "Aggregation",
  "tags": [
    "ST_ConvexHull",
    "ST_Collect"
  ],
  "premium": false,
  "description": "<p>A table of <strong>sensor pings</strong> records GPS points from a fleet of drones, each tagged with a <code>mission_id</code>. For each mission, compute the convex hull polygon enclosing all of its pings, and report its area.</p><h3>Goal</h3><p>Return <code>mission_id</code> and <code>hull_area</code> (planar area of the convex hull), ordered by <code>mission_id</code>.</p><h3>Example Output</h3><pre>mission_id  hull_area\n----------  ---------\n1           50.0\n2           12.5</pre>",
  "schema": "<h3>Table: pings</h3><table class=\"schema-table\"><tr><th>Column</th><th>Type</th><th>Description</th></tr><tr><td>ping_id</td><td>INTEGER</td><td>Primary key</td></tr><tr><td>mission_id</td><td>INTEGER</td><td>Groups pings by mission</td></tr><tr><td>geom</td><td>GEOMETRY</td><td>Point location</td></tr></table><p style=\"margin-top:0.75rem\">You'll need to aggregate points per mission into a single multi-point geometry before computing a hull. <code>ST_Collect</code> takes a LIST of geometries (e.g. via <code>list(geom)</code> after a GROUP BY) and combines them into one.</p>",
  "hints": "<div class=\"hint-block\"><strong>💡 Hint 1</strong> First <code>GROUP BY mission_id</code> and use <code>list(geom)</code> to collect all points per mission into an array.</div><div class=\"hint-block\" style=\"margin-top:0.75rem\"><strong>💡 Hint 2</strong> Wrap that list with <code>ST_Collect(...)</code> to get one multi-geometry, then <code>ST_ConvexHull(...)</code>, then <code>ST_Area(...)</code>.</div><div class=\"hint-block\" style=\"margin-top:0.75rem\"><strong>💡 Hint 3</strong> Full chain: <code>ST_Area(ST_ConvexHull(ST_Collect(list(geom))))</code>.</div>",
  "starterCode": "LOAD spatial;\n\n-- For each mission, compute the area of the convex hull of all its pings.\n-- Return: mission_id, hull_area ordered by mission_id\nSELECT\n  mission_id,\n\nFROM pings\nGROUP BY mission_id\nORDER BY mission_id;",
  "setup": "CREATE TABLE pings AS SELECT * FROM (VALUES\n  (1, 1, ST_Point(0, 0)),\n  (2, 1, ST_Point(10, 0)),\n  (3, 1, ST_Point(10, 10)),\n  (4, 1, ST_Point(0, 10)),\n  (5, 1, ST_Point(5, 5)),\n  (6, 2, ST_Point(0, 0)),\n  (7, 2, ST_Point(5, 0)),\n  (8, 2, ST_Point(5, 5)),\n  (9, 2, ST_Point(0, 5))\n) t(ping_id, mission_id, geom);",
  "expected": [
    {
      "mission_id": 1,
      "hull_area": 100
    },
    {
      "mission_id": 2,
      "hull_area": 25
    }
  ]
}
